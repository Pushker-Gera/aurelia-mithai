# Optional public canonical origin; set to the actual production URL.
NEXT_PUBLIC_SITE_URL=
# Optional server-only HTTPS endpoint. Empty = transparent demo submissions.
AURELIA_FORMS_WEBHOOK=
# Optional bearer secret for the webhook. Never prefix with NEXT_PUBLIC_.
AURELIA_FORMS_SECRET=
